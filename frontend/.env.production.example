# Production env for Vercel (or any Vite host).
# Set this in Vercel → Project → Settings → Environment Variables
# (Production + Preview), or:
#   cp .env.production.example .env.production
#
# Must point at the Render API, including the /api suffix.
# No trailing slash after /api.

VITE_API_URL=https://mini-store-3p9i.onrender.com/api
